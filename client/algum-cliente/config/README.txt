Este diretório guarda os arquivos de configuração do container de aplicação. Aquele que recebe as requisições encaminhadas pelo proxy reverso e se comunica com ele via certificado SSL (HTTPS) auto-assinado (dummycert).

